import React from 'react';
import PropTypes from 'prop-types';

const Bottom = props => {
    return (
        <div>
            This is Bottom
        </div>
    );
};

Bottom.propTypes = {
    
};

export default Bottom;