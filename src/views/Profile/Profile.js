import React from "react";
import PropTypes from "prop-types";

const Profile = (props) => {
  return <div>this is profile page build bang dataInfo</div>;
};

Profile.propTypes = {};

export default Profile;
